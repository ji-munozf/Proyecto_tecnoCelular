import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tecnoCelular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
